#include <stdio.h>
#include <math.h>


main() {
    float x;
    
    printf ("Enter the value of x = ");
    scanf ("%f", &x);
    
    float fx = atan (x);
    printf ("f(x) = arctg(x) = %f\n", fx);
    
    main(&x)
    printf ("f`(x) = (1 / (x^2) + 1) = %f\n", f1x)
    
}

int main1(int *x1) {
    
    float f1x = (1 / (pow(*x1, 2) + 1));
    printf ("f`(x) = (1 / (x^2) + 1) = %f\n", f1x)
    
}
